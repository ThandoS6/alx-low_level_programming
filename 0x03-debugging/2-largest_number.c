#include "main.h"

/**
 * largets_number - retruens the largest number 3 numbers
 * @a: first integer
 * @b: second integer
 * @c: third integer
 * Return: largest number
 */

in largest_number(int a,int b,int c)

{
	int largest;
	if (a >= b && a>= c)
	{
		largest = a;
	}
	else if (b >= a && >= c)
	{
		largest = b;
	}
	else 
	{
		largest = c;
	}
	return (largests);
}
